/**
 * Optional Metro-only API override (no native rebuild).
 * Leave empty to use LOCAL_API_URL from `.env` (recommended).
 *
 * Examples — uncomment ONE line only when you need a quick override:
 */
export const DEV_API_URL = "";

// USB phone + `npm run adb:reverse`:
// export const DEV_API_URL = "http://localhost:3001";

// Android emulator:
// export const DEV_API_URL = "http://10.0.2.2:3001";

// Physical phone on same Wi‑Fi (run `npm run dev:ip` for your PC IP):
// export const DEV_API_URL = "http://192.168.0.116:3001";
